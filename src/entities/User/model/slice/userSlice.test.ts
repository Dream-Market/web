import { UserSchema } from '../types/userSchema';
import { userActions, userReducer } from './userSlice';

describe('userSlice', () => {
  test('Should work with empty state', () => {
    // expect(userReducer(undefined, userActions.action())).toEqual({});
  });
});
