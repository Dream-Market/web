import { createSlice } from '@reduxjs/toolkit';
import { {{pascalCase name}}Schema } from '../types/{{camelCase name}}Schema';

const initialState: {{pascalCase name}}Schema = {};

export const {{camelCase name}}Slice = createSlice({
  name: '{{camelCase name}}',
  initialState,
  reducers: {},
});

export const { actions: {{camelCase name}}Actions } = {{camelCase name}}Slice;
export const { reducer: {{camelCase name}}Reducer } = {{camelCase name}}Slice;
