import { {{pascalCase name}}Schema } from '../types/{{camelCase name}}Schema';
import { {{camelCase name}}Actions, {{camelCase name}}Reducer } from './{{camelCase name}}Slice';

describe('{{camelCase name}}Slice', () => {
  test('Should work with empty state', () => {
    // expect({{camelCase name}}Reducer(undefined, {{camelCase name}}Actions.action())).toEqual({});
  });
});
